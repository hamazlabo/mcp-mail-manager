# CLAUDE.md

このリポジトリで作業するときの行動規律。プロジェクト固有の指示とマージして使う。

## 1. 最小の変更で解く

- 求められていない機能・抽象・設定可能性を足さない。単一利用のコードに抽象を作らない。
- 起こり得ないケースのエラーハンドリングを書かない。
- 200 行書いて 50 行で済むなら書き直す。「シニアエンジニアが過剰だと言うか？」で判断する。

## 2. 外科的に触る

- 隣接するコード・コメント・整形を「ついでに改善」しない。壊れていないものをリファクタしない。
- 自分ならこう書く、より**既存のスタイルに合わせる**。
- 無関係な dead code は削除せず指摘に留める。自分の変更が生んだ孤児（未使用の import・変数・関数）は削除する。
- 判定: **変更した全行が、依頼に直接たどれること**。

## 3. 迷いを隠さない

- 解釈が複数あるなら、黙って 1 つ選ばずに提示する。より単純な方法があるなら言う。
- 不明なまま進めるより、何が不明かを名指しして聞く（ループ実行時は推測せず所定のラベルで停止する）。

## 4. 検証可能な形に落とす

「バリデーションを追加」→「不正入力のテストを書き、通す」。「バグを直す」→「再現するテストを書き、通す」。
複数ステップの作業は、各ステップに検証手段（テスト・アサーションコマンド）を添えてから着手する。

## 5. Spec 駆動開発 (SDD) ステアリング

- 憲法は `.spec/constitution.md`。人間が管理し、エージェントは編集しない。AWS 前提・サーバレス優先・TDD を必ず守る。
- 仕様は `.spec/` 配下（specify → requirements → design → task → adr）。実装は必ず spec を根拠にし、spec にない振る舞いは追加しない。
- spec 作成は `/sdd init <開発要望>`、実装は `/sdd develop [T-xxx]`（`.claude/skills/sdd/`）。
- 設計判断を変える場合は ADR を起票し、design.md と task.md を同期させる。
- 実装中の知見は `.learn/` に記録し（`/sdd learn`）、溜まったら `/sdd upgrade` で本ファイル・手順書・テンプレートへ昇格する。憲法への反映は人間が行う。
