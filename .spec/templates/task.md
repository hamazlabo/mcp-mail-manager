# Tasks: {{システム名}}

> design.md を AI エージェントが 1 タスクずつ実行できる粒度に分解した実装計画。
> `/sdd develop` はこのファイルの未完了タスクを上から順に実行する。
> すべてのタスクは TDD（Red → Green → Refactor）で進める。

## 進め方の規約

- タスク ID は `T-001` 形式。依存関係は ID で示す。
- 1 タスク = 1 つの検証可能な成果。目安は変更 300 行以内、テスト 1〜5 本。
- タスク完了時にチェックボックスを `[x]` にし、完了日を記入する。
- 設計から外れる必要が出たら、タスクを止めて ADR を起票する。

## フェーズ 0: 足場

- [ ] **T-001: {{プロジェクト初期化}}**
  - 目的: {{}}
  - 対応要件: -
  - 対象ファイル: {{}}
  - 最初に書くテスト: {{テストランナーが動くことを確認するダミーテスト}}
  - 完了条件: {{`npm test` が成功する など}}
  - 依存: なし

- [ ] **T-002: CDK 足場と stage コンテキスト**
  - 目的: `cdk deploy --all --context stage=dev|prod` でスタック名が切り替わる
  - 対応要件: NFR-{{IaC}}
  - 対象ファイル: infra/..., cdk.json
  - 最初に書くテスト: CDK assertions で stage ごとにスタック名が変わることを検証する
  - 完了条件: `npx cdk synth --all --context stage=dev` が成功し、テストが通る
  - 依存: T-001

- [ ] **T-003: 正常性テストの足場**
  - 目的: `npm run test:smoke` が `CDK_OUTPUTS_FILE` を読み、デプロイ済み環境へ最小のリクエストを送れる
  - 対応要件: NFR-{{可用性}}
  - 対象ファイル: test/smoke/...
  - 最初に書くテスト: outputs ファイルが無いときに明確なエラーで失敗する
  - 完了条件: ローカルで outputs ファイルを与えて実行できる
  - 依存: T-002

- [ ] **T-004: CI/CD の有効化**
  - 目的: `.github/workflows/README.md` に従い OIDC ロールと Repository variables を設定し、develop への push で dev デプロイと main 昇格が動く
  - 対応要件: NFR-{{IaC}}
  - 対象ファイル: なし（GitHub / AWS 設定）
  - 最初に書くテスト: -（手動確認: deploy-dev が成功し main が更新される）
  - 完了条件: deploy-dev / deploy-prod が green
  - 依存: T-003

## フェーズ 1: {{コア機能}}

- [ ] **T-005: {{タスク名}}**
  - 目的: {{何を実現するか}}
  - 対応要件: REQ-001
  - 対象ファイル: {{src/..., test/...}}
  - 最初に書くテスト: {{テスト名と検証内容}}
  - 完了条件: {{受け入れ基準のどれが満たされるか}}
  - 依存: T-004

- [ ] **T-006: {{タスク名}}**
  - 目的: {{}}
  - 対応要件: {{}}
  - 対象ファイル: {{}}
  - 最初に書くテスト: {{}}
  - 完了条件: {{}}
  - 依存: T-005

## フェーズ 2: {{インフラ / デプロイ}}

- [ ] **T-0xx: {{アプリ固有リソースの追加（DynamoDB テーブル / Lambda など）}}**
  - 目的: {{}}
  - 対応要件: NFR-001
  - 対象ファイル: {{infra/...}}
  - 最初に書くテスト: {{CDK assertions によるリソース存在 / プロパティテスト}}
  - 完了条件: {{テスト成功、develop への push で dev デプロイと正常性テストが通る}}
  - 依存: T-002, {{}}

## 完了ログ

| タスク | 完了日 | 備考 |
|--------|--------|------|
